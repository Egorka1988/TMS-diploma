language: python
python:
  - "3.6"
  - "3.7"
  - "3.8-dev"  # 3.8 development branch

services:
  - postgresql

before_script:
  - touch /home/travis/build/EgorChernik/TMS-diploma/sea_battle/logging/sb_logger_debug.log
  - psql -c 'create database travis_ci_test;' -U postgres

install:
  - pip install --trusted-host pypi.python.org -r requirements.txt

env:
  global:
    - PGPORT=5432

script:
  - pytest
